$kind: http-request
url: "{{base_url}}/login/"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "username": "admin",
      "password": "WrongPassword"
    }
auth:
  type: noauth
order: 3000
